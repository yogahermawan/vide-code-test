{
  "name": "premium-store-cart",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "predev": "npm run build",
    "dev": "vite",
    "build": "esbuild src/main.jsx --bundle --format=iife --minify --define:process.env.NODE_ENV='\"production\"' --outfile=app.js",
    "preview": "vite"
  },
  "dependencies": {
    "react": "^19.2.7",
    "react-dom": "^19.2.7",
    "vite": "^7.3.2"
  }
}
